import React from "react";

const Message = () => {
	// const { id, message } = useAppSelector(selector);
	return (
		// <MessageBuilder
		// 	name={"Greeting"}
		// 	limit={10}
		// 	messages={message}
		// 	chatId={id || 0}
		// />
		<div />
	);
};

export default Message;
